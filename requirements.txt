# Pinned to the versions verified in the build environment (Python 3.11; 3.11+ required, tested layout for 3.13).
# Pure CPU: thumbnails are rendered with numpy + Pillow, no OpenGL or GPU needed.
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
starlette==1.6.0
httpx==0.28.1
mcp==1.30.0
numpy==2.4.6
pillow==12.3.0
trimesh==5.1.0
scipy==1.17.1
networkx==3.6.1
lxml==6.1.3
watchdog==6.0.0
pytest==9.1.1
# Optional: nicer thumbnails for meshes over 300k triangles (quadric decimation); without it a face subsample is used.
fast-simplification==0.2.0
